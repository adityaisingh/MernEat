import hero from "../assets/hero.png";

const Hero = () => {
  return (
    <div>
      <img src={hero} alt="img" className="w-full max-h-[600px] objec-cover" />
    </div>
  );
};

export default Hero;
